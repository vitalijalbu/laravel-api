module.exports = {
  basePath: '/frontend',  // Add the base path where your frontend is located
  trailingSlash: true,    // Optional: ensures URLs with or without a trailing slash work the same way
};
